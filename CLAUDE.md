# CLAUDE.md — CS 공부 자동화 규칙

## 역할
너는 내 CS 공부 파트너야.
내가 "다음" 또는 "공부 시작" 이라고 말하면
`docs/curriculum.md` 를 읽고 자동으로 다음 주제를 골라서 정리해줘.
예시 코드는 **Python** 기준으로 작성해.
수준은 **심화 (면접 대비 / 실무)** 로 작성해.

---

## 자동 진행 로직

### 1. 다음 주제 선택 규칙
- `curriculum.md` 에서 `[ ]` (미완료) 상태인 것 중
- **파트 번호 → 주제 번호** 순으로 가장 앞에 있는 것을 자동 선택
- 내가 따로 주제를 말하면 그걸 우선 처리

### 2. 완료 처리
- 정리가 끝나면 `curriculum.md` 해당 항목을 `[x]` 로 변경

### 3. 누락 주제 감지
- 정리하다가 연관 개념인데 curriculum.md에 없는 주제가 있으면
- 해당 파트의 우선순위에 맞는 위치에 자동으로 추가하고
- 번호를 재정렬해서 curriculum.md 업데이트

---

## 파일 저장 규칙
- 파일 경로: `docs/[파트폴더]/[번호]-[주제명].md`
- 저장 후 자동으로 `git add + commit`
- commit 메시지 형식: `docs: [번호] [주제명] 정리`
- push는 내가 직접 할게

### 폴더 구조
```
docs/
├── curriculum.md
├── 01-network/
├── 02-os/
├── 03-data-structure/
└── 04-db/
```

---

## md 파일 작성 형식

```markdown
# [번호] [주제명]

## 개념
핵심 개념을 면접에서 바로 말할 수 있는 수준으로 설명

## 동작 원리
내부적으로 어떻게 동작하는지 설명

## 예시 코드 (Python)
개념을 직접 구현하거나 활용하는 코드

## 면접 예상 질문
- Q: 질문
  A: 답변

## 관련 개념
연결되는 다른 주제 번호와 링크
```

---

## 자동 승인 규칙
아래는 내 확인 없이 바로 실행:
- 파일 생성 / 수정
- curriculum.md 체크박스 업데이트 / 항목 추가
- git add / commit

아래는 반드시 내 확인 후 진행:
- git push
- Pull Request 생성

---

## 트리거 키워드
| 내가 말하는 것 | Claude Code 동작 |
|---|---|
| `다음` | 다음 미완료 주제 자동 선택 후 정리 |
| `공부 시작` | 01-01부터 순서대로 시작 |
| `[번호] 정리해줘` | 해당 번호 주제 바로 정리 |
| `진행 상황` | curriculum.md 완료/미완료 현황 출력 |
